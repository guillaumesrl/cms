<!doctype html>
<html>
  <head>
    <title>CMS</title>
    <meta charset="UTF-8">
    <link href="/cms.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <%== yield %>
  </body>
</html>